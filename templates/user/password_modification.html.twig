{% extends "base.html.twig" %}

{% block title %} {{ parent()}} | Modif du mdp {% endblock %}

{% block h1 %}Modif du mdp{% endblock %}

{% block body %}

<a href="{{ path("profil") }}" class="btn btn-danger">Retour profil</a>
<div class="col-md-6 mx-auto">

        {{ form_start(formPassword) }}

        {{ form_row(formPassword.oldPassword) }}

        {{ form_row(formPassword.newPassword) }}

        {{ form_row(formPassword.confirmPassword) }}

        <input type="submit" value="modifier" class="btn btn-primary col-md-12 mt-3">

        {{ form_end(formPassword) }}

{% endblock %}